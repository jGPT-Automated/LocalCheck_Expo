/**
 * Retired entry point. Add Court is now an Expo Router full-screen flow at
 * /add-court; this module remains as a source-contract marker for older
 * repository checks and must not be imported by the app.
 */
export {};
