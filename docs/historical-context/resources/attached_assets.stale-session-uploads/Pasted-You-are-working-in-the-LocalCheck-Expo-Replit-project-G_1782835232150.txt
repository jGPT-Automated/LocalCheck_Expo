You are working in the LocalCheck Expo/Replit project.

Goal:
Add the real Supabase backend foundation needed for App Store launch:
- email/password sign up
- email/password sign in
- Sign in with Apple
- persisted auth session
- profile creation/fetch
- one real Supabase data path proving the app is no longer local-only

Replit Secrets already exist:
- EXPO_PUBLIC_SUPABASE_URL
- EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY
- EXPO_PUBLIC_MAPBOX_TOKEN

Do not commit secret values.
Keep the current UI/design intact.

Implement:

1. Install backend/auth dependencies
Add:
- @supabase/supabase-js
- react-native-url-polyfill
- expo-secure-store
- expo-apple-authentication
- expo-crypto if needed for Apple nonce handling

Use Expo-compatible install commands.

2. Create Supabase client
Create:
- artifacts/mobile/lib/supabase.ts

Requirements:
- import react-native-url-polyfill/auto
- read EXPO_PUBLIC_SUPABASE_URL
- read EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY
- use Expo SecureStore-backed auth storage
- persist session
- auto refresh token
- no service-role key
- no database connection string

3. Create AuthContext
Create:
- artifacts/mobile/context/AuthContext.tsx

Expose:
- session
- user
- profile
- isLoading
- signUpWithEmail(email, password, displayName?)
- signInWithEmail(email, password)
- signInWithApple()
- signOut()
- ensureProfile()

Behavior:
- restore session on app launch
- listen for auth state changes
- after sign-up/sign-in, fetch profile from `profiles` where `id = user.id`
- if missing, create a minimal profile row
- do not block the whole app behind auth yet

4. Add AuthProvider
Update:
- artifacts/mobile/app/_layout.tsx

Wrap the app with AuthProvider inside existing providers.

5. Add auth screen
Create:
- artifacts/mobile/app/auth.tsx

Simple screen only:
- email input
- password input
- sign in button
- sign up button
- Sign in with Apple button on iOS only
- sign out button if already signed in
- show current auth/profile status for verification

No redesign. Just make auth usable.

6. Configure Apple Sign-In
Update app config as needed:
- app name: LocalCheck
- iOS bundle identifier: com.realjess.localcheck
- scheme: localcheck
- enable Apple Sign-In capability/plugin config required by Expo

Use native Apple auth with Supabase:
- call expo-apple-authentication
- pass Apple identity token to Supabase with `signInWithIdToken`
- use nonce handling if needed

7. Add one backend data path
Create:
- artifacts/mobile/services/courtService.ts

Load courts from Supabase:
- first try `courts_with_stats`
- if unavailable, try `courts`
- map DB rows into the current Expo Court type

Update AppContext startup:
- try loading courts from Supabase
- if Supabase fails or returns no rows, keep existing local/sample courts fallback
- app must never go blank

8. Verify Mapbox only
Do not refactor Mapbox.
Just confirm the map still renders with EXPO_PUBLIC_MAPBOX_TOKEN.

9. Add docs
Create/update:
- .env.example with placeholder names only
- docs/BACKEND_STATUS.md

Include:
- auth implemented
- Apple Sign-In status
- courts backend read status
- exact Supabase table/view used
- exact commands run
- exact errors if any
- what remains for TestFlight/App Store

Acceptance criteria:
- app renders
- Mapbox still renders
- email sign-up works
- email sign-in works
- session persists after refresh/restart
- user appears in Supabase Auth
- profile row is created/fetched
- Apple Sign-In button appears on iOS
- Apple Sign-In implementation compiles
- courts load from Supabase if available
- local fallback courts still work
- typecheck passes or exact errors are reported

Branch:
replit/backend-auth-appstore-foundation