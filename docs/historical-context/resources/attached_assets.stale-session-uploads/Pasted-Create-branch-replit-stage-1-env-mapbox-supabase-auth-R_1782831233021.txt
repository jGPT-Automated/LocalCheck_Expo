Create branch:
replit/stage-1-env-mapbox-supabase-auth

Repo:
agenticjess-star/LocalCheck_Expo

Goal:
Move LocalCheck Expo from “good local demo” to “shareable app foundation” without expanding scope.

Hard constraints:
- Expo app is the shipping base.
- iOS repo is reference only.
- Supabase Auth only.
- Do not use Better Auth.
- Do not install skills.
- Do not modify .agents/skills, skills-lock.json, attached_assets, or mockup-sandbox.
- Do not wire courts, check-ins, feed, friends, realtime, push, schedule, AI verification, or Apple Sign-In.
- Do not commit real secrets.
- Do not use service role keys or Postgres connection strings in mobile/client code.
- Keep existing design and map clustering.

Implement:

1. Mapbox env visibility
- Inspect artifacts/mobile/components/MapScreen.tsx.
- Keep react-native-maps and UrlTile.
- Do not add native Mapbox SDK.
- Add dev-only safe diagnostic logging:
  - token exists boolean
  - token length
  - token prefix only, first 8–12 chars max
  - never log full token
- Add Expo config fallback:
  - create/convert to app.config.ts if needed
  - expose extra.mapboxToken from process.env.EXPO_PUBLIC_MAPBOX_TOKEN
- Update MapScreen token read order:
  - process.env.EXPO_PUBLIC_MAPBOX_TOKEN
  - Constants.expoConfig?.extra?.mapboxToken
- Preserve existing clustering and visual design.
- Document exact restart/cache command:
  - pnpm --filter @workspace/mobile exec expo start -c

2. App config basics
- App name: LocalCheck
- Scheme: localcheck
- iOS bundleIdentifier: com.realjess.localcheck
- Keep supportsTablet false.
- Do not wire Apple Sign-In yet.

3. Supabase auth foundation
Install/add only:
- @supabase/supabase-js
- react-native-url-polyfill
- expo-secure-store

Create:
- artifacts/mobile/lib/supabase.ts
- artifacts/mobile/context/AuthContext.tsx

supabase.ts:
- import react-native-url-polyfill/auto
- create Supabase client from:
  - EXPO_PUBLIC_SUPABASE_URL
  - EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY
- use SecureStore-backed storage for sessions
- persistSession true
- autoRefreshToken true
- detectSessionInUrl false

AuthContext should expose:
- session
- user
- profile
- isLoading
- signInWithEmail(email, password)
- signUpWithEmail(email, password, displayName?)
- signOut()
- ensureProfile(user?)

Profile behavior:
- After sign-up/sign-in/session restore, fetch profile from public.profiles.
- If missing, create profile with:
  - id = auth user id
  - email = user.email
  - display_name = provided display name OR email prefix OR "Player"
  - username = safe generated username using email prefix + short user id suffix
- Must be idempotent.
- Must respect RLS.
- Use Supabase Auth user ID as profiles.id.

4. Wire provider only
- Add AuthProvider in artifacts/mobile/app/_layout.tsx.
- Do not change screens to require login yet unless needed for compile.
- Keep AppProvider intact for now.

5. Docs
Add:
- .env.example with placeholders only
- docs/APP_STORE_READINESS.md

.env.example placeholders:
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
EXPO_PUBLIC_MAPBOX_TOKEN=
SESSION_SECRET=
AI_INTEGRATIONS_OPENAI_API_KEY=
AI_INTEGRATIONS_OPENAI_BASE_URL=

APP_STORE_READINESS.md should track:
- current stage
- env requirements
- app config values
- what is wired
- what is intentionally not wired
- remaining launch blockers

6. Verification
Run:
- pnpm install
- pnpm run typecheck
- pnpm --filter @workspace/mobile exec expo start -c

Report:
- exact files changed
- exact commands run
- exact errors
- whether app renders
- whether Mapbox token is visible safely
- whether Mapbox renders
- whether Supabase auth code compiles

Stop after Stage 1. Do not expand scope.