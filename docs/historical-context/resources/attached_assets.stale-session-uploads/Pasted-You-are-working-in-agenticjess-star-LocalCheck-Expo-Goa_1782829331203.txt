You are working in `agenticjess-star/LocalCheck_Expo`.

Goal for this stage:
Make material progress toward App Store readiness by first fixing environment-variable plumbing, then adding Supabase auth foundation only. Do not wire all courts/check-ins/feed yet.

Current status:
- Friends/settings button bug was fixed by adding pointerEvents to the invisible sticky header overlay.
- The app currently shows “MAPBOX KEY NEEDED” on the Replit mobile/web preview even though Replit secrets are set.
- The Mapbox key worked in the original Replit project/account.
- This is a remixed Replit project/account.
- Env variables have been set in Replit Secrets already:
  - SESSION_SECRET
  - EXPO_PUBLIC_SUPABASE_URL
  - EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY
  - AI_INTEGRATIONS_OPENAI_API_KEY
  - AI_INTEGRATIONS_OPENAI_BASE_URL
  - EXPO_PUBLIC_MAPBOX_TOKEN

Important:
- Do not commit real secret values.
- Do not paste full secrets into files.
- Do not use the Postgres connection string in mobile/client code.
- Do not use service-role keys.
- Do not redesign the app.
- Do not rewrite the Map UI.
- Do not run database migrations.
- Do not remove AsyncStorage yet.

Step 1 — Mapbox env diagnosis/fix:
1. Inspect `artifacts/mobile/components/MapScreen.tsx`.
2. Confirm how `EXPO_PUBLIC_MAPBOX_TOKEN` is read.
3. The app currently uses:
   `process.env.EXPO_PUBLIC_MAPBOX_TOKEN`
   and sets `HAS_TOKEN` based on that.
4. Add a safe runtime diagnostic in development only:
   - log whether the token exists
   - log token length
   - log token prefix only, e.g. first 3 chars
   - never log the full token
5. Verify if Replit/Expo web preview sees the env var.
6. If the env var is not visible to the Expo client bundle:
   - restart/rebuild the Expo dev server
   - clear Expo/Replit cache if needed
   - document exact command used
7. If still not visible, add robust Expo config support:
   - create or convert to `app.config.ts` if needed
   - expose public runtime config through Expo `extra`
   - update MapScreen to read from:
     - `process.env.EXPO_PUBLIC_MAPBOX_TOKEN`
     - fallback: `Constants.expoConfig?.extra?.mapboxToken`
   - keep `EXPO_PUBLIC_MAPBOX_TOKEN` as the source env var
8. Do not change the visual map design.
9. Do not replace `react-native-maps`.
10. Do not add native Mapbox SDK right now.
11. Keep existing clustering code. It already exists and should not be rewritten.

Mapbox acceptance criteria:
- Replit web/mobile preview no longer shows “MAPBOX KEY NEEDED” when the secret exists.
- Dark Mapbox tiles render.
- If the token is missing, the app shows the current helpful fallback message.
- Dev diagnostic logs do not expose the full token.
- Exact cause is documented in the final response.

Step 2 — App config readiness:
Update app config only if needed for env support and App Store readiness.

Required values:
- App name: `LocalCheck`
- iOS bundle identifier: `com.realjess.localcheck`
- Scheme: `localcheck`

Do not configure Apple Sign-In yet unless it is already scaffolded and trivial. This stage is not Apple Sign-In.

Step 3 — Supabase auth foundation only:
Install/add only what is required for Supabase auth foundation:
- `@supabase/supabase-js`
- `react-native-url-polyfill`
- `expo-secure-store`

Create:
- `artifacts/mobile/lib/supabase.ts`
- `artifacts/mobile/context/AuthContext.tsx`

Supabase client requirements:
- Use `EXPO_PUBLIC_SUPABASE_URL`
- Use `EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY`
- Support React Native session persistence.
- Use secure/persistent storage where appropriate.
- Include `react-native-url-polyfill/auto`.
- No service-role key.
- No database URL.
- No hardcoded private secrets.

AuthContext requirements:
Expose:
- `session`
- `user`
- `profile`
- `isLoading`
- `signInWithEmail`
- `signUpWithEmail`
- `signOut`
- `ensureProfile`

Profile behavior:
- After sign-up/sign-in, fetch profile from `profiles`.
- If missing, create profile for the authenticated user.
- Use `auth.uid()`/authenticated user ID as `profiles.id`.
- Do not require Apple Sign-In yet.

Do not wire:
- courts
- check-ins
- feed
- schedule
- friends
- realtime
- push notifications
- AI court verification

Keep current `AppContext` and AsyncStorage logic intact for now.

Step 4 — docs:
Add/update:
- `.env.example`
- `docs/APP_STORE_READINESS.md`

`.env.example` should include placeholder values only:
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
EXPO_PUBLIC_MAPBOX_TOKEN=
AI_INTEGRATIONS_OPENAI_API_KEY=
AI_INTEGRATIONS_OPENAI_BASE_URL=
SESSION_SECRET=

Docs should note:
- Replit secrets are already set.
- Mapbox env issue was diagnosed/fixed.
- Supabase auth foundation exists.
- Courts/check-ins/feed are intentionally deferred.
- Apple Sign-In is deferred to a later stage.
- App Store blockers still remaining.

Step 5 — verification:
Run:
- install command if needed
- typecheck command if available
- Expo web/mobile preview
- verify Mapbox renders
- verify auth code compiles
- verify app still renders existing UI

Report back with:
- exact files changed
- exact commands run
- exact errors, if any
- whether Mapbox token was visible at runtime
- whether Mapbox renders now
- whether Supabase auth code compiles
- what remains for the next stage

Branch:
`replit/stage-1-env-mapbox-supabase-auth`

Commit separately from previous sticky header fix.